# Leads-Adaptive-Network-for-fECG
we  propose a lead-adaptive DL network architecture.  The model can be trained on data collected from multiple  leads and supports inference with a reduced number of leads.
